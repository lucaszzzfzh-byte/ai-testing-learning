# Phase 1 & 2 学习笔记

---

## Phase 1（5.28 - 5.30）— AI 测试基础

### Day 1（5.28）— API 调用入门

用 Anthropic SDK 调 DeepSeek 的 `/anthropic` 端点，写了 `lesson1_prompt.py`。

**收获**：
- API Key、base_url、model 三要素配置
- SDK 本质上是给你装好 requests 的壳，底层还是 HTTP POST
- DeepSeek Anthropic 端点 `thinking` 默认开启 → 必须加 `thinking={"type": "disabled"}`
- `temperature` 控制输出稳定性（0.3 = 稳，0.9 = 发散）
- 从报错定位语法问题的能力

### Day 2（5.29）— Python + Dify 平台搭建

**Python 部分**：
- `json.dumps` = 门票：dict → 格式化文本字符串
- `json.loads` = 回执：字符串 → 可操作的字典
- `sort_keys=True` 按 Unicode 码点排序
- 三种不可序列化类型：datetime（用 `str()`）、set（用 `list()`）、自定义类（用 `.__dict__`）

**Dify 部分**：
- 配置 DeepSeek 模型提供商（OpenAI-API-compatible，端点 `https://api.deepseek.com/v1`）
- Chat Assistant vs Agent vs Chatflow 三种应用类型区别
- 搭建知识库 + 接入 WebSearch 工具
- 解决 "Model not supported" 报错

### Day 3（5.30）— Dify Agent 实操 + GitHub

- 创建 Agent「AI测试用例生成助手」+ 编写系统提示词
- 两条需求测试：登录功能（6 条结构化用例）、购物车功能
- 截图 + 写文档 + Git add/commit/push 提交 GitHub

### 技能清单（Phase 1 结束）

| 分类 | 掌握的 |
|------|--------|
| API 调用 | Anthropic SDK + DeepSeek 端点，报错排查 |
| Python | json.dumps/loads、dict/list/str 类型区分、for 循环 |
| Dify | 模型配置、Agent 创建、知识库、WebSearch 工具 |
| Git/GitHub | git add/commit/push、仓库管理 |
| 测试思维 | 提示词工程基础（角色设定、输出格式、规则约束） |

---

## Phase 2（6.1 - 6.4）— 测试设计方法 + Python

### 6/1 — 等价类 + 边界值 + Python requests

**等价类划分法**：把输入分成有效/无效等价类，每类选一个代表值，避免冗余测试。

**边界值分析法**：bug 大多藏在边界，在等价类边界上取 ±1 三个点。每个边界取：边界-1、边界本身、边界+1。

两个方法通常合着用：等价类分类 → 边界值定点。

**Python requests**：
- `requests.get()` / `requests.post()`，`response.status_code` / `response.json()`
- params 和 headers 的传法
- 用 raw requests 调 DeepSeek OpenAI 兼容端点（`/v1/chat/completions`），写 `deepseek_requests.py`
- SDK vs raw requests：SDK 就是 requests + 便利封装

### 6/2 — 判定表法 + 函数封装

**判定表法**：多条件组合时穷举所有规则，合并结果相同的列，精简测试用例。

四要素：
- 条件桩（列所有条件，左上）
- 动作桩（列所有结果，左下）
- 条件项（每个条件的取值组合，右上）
- 动作项（对应组合的结果，右下）

步骤：列条件 → 穷举 2^n 条规则 → 填结果 → 合并简化 → 删矛盾。

**Python 函数封装**：
- `def 函数名(参数):` 结构
- `isinstance` 类型检查
- 多值返回（元组拆包 `a, b = func()`）
- 默认参数
- for 循环替代重复代码
- 把等价类+边界值判断封装成 `check_score(score)` 函数（`python_practice3.py` / `python_practice3.1.py`）

### 6/3 — 因果图法

判定表的前置步骤。画条件间的逻辑连接和约束关系，避免穷举出不可能存在的组合。

**四种因果连接**：

| 连接 | 含义 |
|------|------|
| 恒等 | 因发生 → 果发生 |
| 非 | 因发生 → 果不发生 |
| 与 | 所有因同时发生 → 果发生 |
| 或 | 任一因发生 → 果发生 |

**两种约束**：

| 约束 | 含义 |
|------|------|
| E（互斥） | 两个条件只能一个为真 |
| I（包含） | 至少有一个为真 |

**步骤**：找因和果 → 画连线 → 标约束 → 转判定表 → 删违反约束的列 → 出用例。

### 6/4 — 正交实验法 + 状态迁移法

**正交实验法**：条件太多、组合爆炸时用正交表（L_9(3^4) 等），确保任意两列取值搭配均匀，9 条覆盖 81 条的工作量，省 89%。

正交表命名：L_行数(取值数^条件数)。核心保证：任意两列之间，所有取值组合出现次数相同（两两均匀覆盖）。

适用：条件 ≥ 4 个，或条件 3 个但取值 ≥ 3 种。

**状态迁移法**：跳出输入思维，测系统在不同状态之间的跳转路径。

三要素：
- 状态（圆圈） — 系统某个稳定情况
- 迁移（箭头） — 什么事件触发状态变化
- 守护条件 — 迁移发生的条件

步骤：识别状态 → 画迁移箭头 → 覆盖每个状态 → 覆盖每条迁移 → 补充非法迁移。

例子（密码锁定）：5 条迁移：
1. 正常 → 正常（输对密码，归零）
2. 正常 → 正常（输错1~2次，累加）
3. 正常 → 锁定（连续3次错误）
4. 锁定 → 锁定（锁定期间任意操作）
5. 锁定 → 正常（30分钟后自动恢复）

---

## 六种测试设计方法总结

### 各方法概要

| # | 方法 | 核心思路 | 适用场景 |
|---|------|---------|---------|
| 1 | 等价类划分 | 分成有效/无效类，每类选代表 | 单条件输入 |
| 2 | 边界值分析 | 边界 ±1 三个点定点 | 任何有取值范围的输入 |
| 3 | 判定表 | 多条件穷举 → 合并简化 | 2~4 个条件组合 |
| 4 | 因果图 | 画条件约束 → 转判定表 | 条件间有逻辑依赖时 |
| 5 | 正交实验 | 用正交表两两均匀覆盖 | 条件 ≥ 4 个或取值 ≥ 3 种 |
| 6 | 状态迁移 | 画状态图和迁移路径 | 系统有明显状态流转 |

### 方法之间的关系

```
等价类（单条件分类）
  → 边界值（在分类边界上定点）
    → 判定表（多条件交叉组合）
      → 因果图（加条件间逻辑约束）
        → 正交实验（条件太多时用正交表压缩）
          → 状态迁移（跳出输入思维，测状态流转）
```

前五种关注"输入怎么组合"，第六种关注"系统怎么流转"。
